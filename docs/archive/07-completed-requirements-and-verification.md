# 已完成需求与验证记录（归档）

## 已完成需求

### 已有帕鲁预设与可编辑配种图（REQ-002）

2026-08-01 完成以可编辑配种图替换旧自动路径规划的五阶段重构：

- 预设和方案相互独立并支持多对多关联；预设提供完整帕鲁网格、统一搜索、批量选择、显式保存和全局未保存保护，方案提供独立生命周期与 500ms 自动保存。
- 配种森林支持同帕鲁重复节点、精确配方关系、确定性分层布局和等价文本列表；完整工具覆盖创建子代、查询获取方式、合并、删除、100 条会话内撤销/重做、自动整理、缩放及适应画布。
- “子代反查亲本”更名为“获取目标帕鲁”；精确双亲、单亲展开和目标反查配方均可追加到当前方案，并保留方案、预设和视口返回上下文。
- `.paltools-plan.json` 导入导出限制为 5 MiB、1,000 节点和 1,000 关系；导入整体重建 ID、避让方案名称、不绑定预设，并按当前配方索引重映射和校验关系，失败时不产生部分写入。
- IndexedDB 保持 `paltools-breeding` v1；旧 `paltools.path-starts.v1` 只读迁移且不主动删除。静态数据 Schema、无性别配方、图鉴搜索规则和 Electron 安全配置均未改变。

最终验收：22 个测试文件、174 项测试全部通过，TypeScript、300 个帕鲁/44,851 条配方的数据校验和 Vite 生产构建成功。真实 Chromium 覆盖八套主题、五种桌面/窄屏/低高度视口、键盘替代操作、方案导入导出和 500 节点性能；无横向溢出、可见破图、控制台警告/错误或第三方请求。便携包 `PalTools-0.1.0-win-x64.exe` 为 87,718,822 字节，SHA-256 为 `448E539E573436EDB26E71C8AB9A454C110F4370018E4B51B107061E884FE3A4`，打包应用 smoke 通过。

### 图鉴与配种关系 CLI 工具（REQ-004）

2026-07-31 完成独立 `cli/` 模块：图鉴与配种关系检索支持中英文名、拼音、内部 ID、paldb ID 和图鉴编号（含前导零）身份解析；提供 `info`、`search`、`forward`、`reverse` 与 `plan validate` 命令；默认离线读取 `public/data` 的 Schema v4 JSON，支持 `--json` 稳定输出、`--data-dir` 与 `PALTOOLS_DATA_DIR`。

- `search` 支持查询词及 `--element`、`--work`、`--sort`、`--dir`、`--limit`；文本模式输出表格，`--json` 输出稳定结构。
- 退出码：0 成功；1 方案校验失败或内部错误；2 参数错误或身份歧义；3 无结果；4 数据缺失或 Schema 不兼容。
- `plan validate` 读取 `.paltools-plan.json` 导出信封并复用领域校验，同时报告导出与当前数据集的版本差异。
- 开发期 `npm run cli` 经 `tsx` 复用 `src/domain/*`；`npm run cli:build` 用 esbuild 生成单个 Node ESM 文件 `build/cli/paltools.mjs` 并注入应用版本，Windows 使用 `script/paltools.cmd` 包装，macOS/Linux 通过同一 `.mjs` 运行；便携 EXE 分发继续评估。
- CLI 与 Web/Electron 共用领域逻辑、Schema v4 和版本信息，不引入运行时网络请求。

### 新增三套主题（REQ-003）

2026-07-31 完成薰衣草霓虹、珊瑚莓果、深海薄荷三套主题：

- 薰衣草霓虹：深紫背景 `#211A3A`、薰衣草紫 `#B79CFF`、电光青 `#44E5D0`，突出科技感和高对比度。
- 珊瑚莓果：暖白背景 `#FFF4EE`、珊瑚红 `#FF6B6B`、莓果紫 `#B83B7E`，突出明快、活泼的编辑型配色；小号强调文字使用满足 4.5:1 的 `#A62F6E`。
- 深海薄荷：深海靛蓝 `#102A43`、薄荷绿 `#5EEAD4`、海盐青 `#A7F3D0`，突出清爽、沉浸的海洋型配色。
- 三套主题补齐完整语义令牌；主题注册表、键盘切换、刷新恢复、桌面/窄屏布局沿用现有机制，业务组件不感知主题 ID。
- 主题测试扩展为全令牌覆盖并加入 WCAG 对比度断言（正文 4.5:1、控件边界 3:1），同时修正 sky 强调色与 lavender、coral 控件边框的对比度不足。

### 界面颜色主题更换

2026-07-29 完成可扩展的语义主题系统：

- 提供森林夜色、珍珠白、石墨灰、晴空浅蓝和琥珀橙五套主题，森林夜色保持默认视觉。
- 使用 `paltools.theme.v1` 保存版本化偏好，并在 React 首次渲染前应用，避免启动闪烁。
- 主题注册表是设置页的唯一数据源；增加主题时只需补充注册项和完整令牌块，业务组件不感知主题 ID。
- CSS 按主题、基础、共享、特性和工具层固定加载；图鉴、配种、详情和设置样式已物理拆分。
- 同期将原 1,202 行 `App.tsx` 拆为应用壳、特性页面、共享组件及数据/配置/偏好 Hook，并保留原数据 Schema、搜索规则、配种算法和 Electron 安全设置。
- 单元与组件测试覆盖偏好解析、回退、注册唯一性、五主题卡片、键盘切换、即时持久化及高级设置兼容性；真实浏览器覆盖五主题、桌面/窄屏、关键页面、刷新恢复、溢出、破图、控制台和本机请求边界。

最终发布门通过：11 个测试文件、52 个测试通过，TypeScript、数据校验、Vite 构建、electron-builder 和打包应用 smoke 均成功。主题重构后的 `PalTools-0.1.0-win-x64.exe` 为 87,695,608 字节，SHA-256 为 `CA5B107A7F590BC4E84E50F1876DDAFD0A37083B0F52453C9AE3527A96EF2938`；相对 87,692,256 字节轻量化基线增加 3,352 字节（0.00382%），可视为体积基本持平。

### 最终打包产物轻量化

2026-07-29 在保留 Electron、Windows x64 便携单 EXE、完整离线能力和原有安全设置的前提下完成首轮低风险优化：

- Electron 语言包由 55 个缩减为 `zh-CN`、`en-US` 两个。
- `pinyin-pro` 作为 Vite 构建期依赖保留，不再以生产 Node 包重复进入 ASAR。
- 保留 Chromium/Electron 许可证、GPU/Vulkan 回退库和公共运行时数据，避免合规或硬件兼容风险。
- `package:exe` 增加语言包集合断言，并输出精确字节数和 SHA-256。

同一代码提交下的打包对比：

| 指标 | 优化前 | 优化后 | 变化 |
| --- | ---: | ---: | ---: |
| 便携 EXE | 95,391,372 字节（90.97 MiB） | 87,692,256 字节（83.63 MiB） | -7,699,116 字节（-8.07%） |
| 解包内容 | 373,575,540 字节（356.27 MiB） | 324,910,122 字节（309.86 MiB） | -46.41 MiB（-13.03%） |
| Electron 语言包 | 55 个 | 2 个 | 保留中文和英文回退 |

最终产物：

- 文件名：`PalTools-0.1.0-win-x64.exe`
- SHA-256：`416193150873AEC8BA20FF28E2DB0623A476F2B5E8FF2AAFF3923F64D44B57D6`
- 发布门：300 个帕鲁、44,851 条配方的数据校验通过；7 个测试文件、39 个测试通过；TypeScript、Vite 构建和打包应用 smoke 通过。

已知限制：Vite 主 JS chunk 为 692.73 kB，并产生超过 500 kB 的构建警告；代码拆分可改善首屏加载，但不会显著降低经过固实压缩后的 EXE 总体积，因此不纳入本轮轻量化。

## 已完成问题修复

### 图鉴详情弹窗布局、主题背景和搜索框拼写检查

- `BUG-007`：详情弹窗关闭按钮提升到弹窗内部最上层，并扩大点击区域，避免被主动技能栏或滚动内容覆盖。
- `BUG-008`：主动技能标题取消异常吸顶并向右微调；详情弹窗和主动技能大面板增加圆角、边框与层次，弹窗渐变、表面、阴影、焦点和滚动条均改用主题令牌，切换五套主题后不再残留森林绿色。
- `BUG-009`：图鉴搜索、帕鲁选择器、单亲配方筛选和反向亲本筛选全部禁用浏览器拼写检查，避免中文、拼音和内部 ID 出现无意义的拼写标记。

### 属性外观、应用版本与默认排序文案

- `BUG-001`：属性标签、边框、表面和筛选状态改用五套主题均完整定义的属性语义令牌；火、水、雷等来源图标继续保留身份色。
- `BUG-002`：应用版本统一由 `package.json` 经 `VITE_APP_VERSION` 构建变量注入，右上角只显示应用版本；数据集版本继续由 manifest 独立维护。
- `BUG-003`：排序选项显示为“图鉴编号”，默认升序、方向切换和重置行为保持不变。

### 工作适性、配种卡片主题与单亲配方查询

- `BUG-004`：工作适性筛选按钮和图鉴卡片徽章改用五套主题完整定义的文字、边框及表面令牌；珍珠白与晴空浅蓝不再复用固定深色背景。
- `BUG-005`：配种卡片的边框、底色和双层渐变改用主题配方令牌，主题切换后渐变、表面与强调色同步变化。
- `BUG-006`：双亲查询只选择 A 或 B 时均可展开该帕鲁参与的全部无性别配方，并支持另一亲本/子代统一身份搜索、确定性排序、匹配/总数提示、每页 50 条分页及无结果状态；选择两只亲本时继续执行精确查询。

### 搜索支持不完整拼音和纯数字图鉴编号

- 中文名称会生成无声调连续拼音和拼音首字母别名，支持大小写不敏感的连续片段匹配。
- 纯数字查询可匹配去除前导零后的图鉴编号，并保留字母后缀条目的前缀匹配。
- 图鉴搜索、配种选择框和反向亲本筛选共用身份搜索规则。
- 中文名、英文名、内部 ID、技能、掉落物、属性和工作适性能力保持不变。

### 宽屏顶部栏左右过渡

- 顶部栏内容继续与 1440px 主内容区对齐，背景、模糊层和底部边界延伸到整个视口宽度。
- 常见桌面宽度、125% 缩放有效视口和窄屏布局不产生横向溢出。

### 稀有度五角星展示

- 稀有度固定使用一行 5 颗星展示；1–5 级使用黄色实心星和空心星。
- 6–10 级从左向右用彩色渐变星覆盖黄色星；现有数据中高于 10 的条目饱和显示为 5 颗彩色星，同时保留原始数值的无障碍文本。
- 缺失稀有度时继续显示“暂无数据”。

### “固有词条”命名与空状态

- 详情页将“固有被动技能”统一显示为“固有词条”。
- 数据为 `null` 或空数组时不渲染标题、空状态或空白区块。
- 有词条时继续展示名称、Rank 和描述。

### 详情弹窗左右栏独立滚动

- 桌面布局下，左侧详情与右侧主动技能支持独立上下滚动。
- 滚动一侧不会带动另一侧、整个弹窗或背景页面。
- 低高度和窄屏下回退为弹窗内部整体滚动，确保全部内容可访问。
- 滚动条按悬停、键盘聚焦和滚动状态显示，并保持内容布局稳定。

### 固有词条区域视觉错位

- 固有词条、工作适性和掉落物品区块保持稳定排列。
- 标题、卡片、Rank 和多行描述不再错位、重叠或产生异常留白。
- 无词条时隐藏区块；有一项和多项词条时布局均保持一致。

### 默认顺序支持调整排列方向

- 默认顺序按图鉴编号处理，并支持升序和降序。
- 无图鉴编号的条目在两个方向下均稳定排在有效编号之后。
- 搜索和筛选变化后保留当前排列方向，重置后恢复图鉴编号升序。

### 重置按钮视觉不够醒目

- 图鉴筛选区的重置按钮无需悬停即可识别。
- 默认、悬停、按下和键盘聚焦状态均清晰可辨。
- 桌面和窄屏下不会挤压相邻排序控件。

### 配种结果卡片重复展示子代名称

- 移除卡片底部重复的子代摘要区域。
- 正向配种和反向查询的公式布局均保持清晰。
- 子代头像、名称、角色和完整可访问文本仍然保留。

### 目标子代选择框过长

- 宽屏下目标子代选择框不再占据过多横向空间。
- 候选面板仍可展示完整名称、英文名和图鉴编号，并支持滚动与键盘选择。
- 1366×768 和窄屏下无水平溢出，控件可恢复全宽。

### 应用品牌图标

- Electron 应用和便携 EXE 使用新的关联图标。
- 顶栏使用 `public/app-icon-96.png`，favicon 使用 `public/favicon-32.png`。
- Windows 多尺寸图标位于 `script/electron/resources/icon.ico`，母版位于 `script/electron/resources/icon.png`。
- 图标来源、提示词和派生流程已记录在数据与合规文档中。

## 历史验证记录

### 2026-07-28

- 定点测试：`src/domain/pals.test.ts`、`src/App.test.tsx` 通过。
- 完整测试、数据校验、TypeScript 类型检查和 Vite 生产构建通过。
- 浏览器回归覆盖桌面、低高度和窄屏尺寸；确认无水平溢出、滚动穿透、控制台错误或破图。
- Electron 打包应用隐藏 smoke 通过。

### 2026-07-29

- Bug 修复轮次最终为 7 个测试文件、39 个测试全部通过。
- `npm.cmd run build` 通过，包含 300 个帕鲁、44,851 条配方的数据校验、TypeScript 类型检查和 Vite 生产构建。
- Playwright 回归覆盖 1920×1080、1440×900、1366×768、1152×720 和 390×844；确认顶部栏、页面与详情均无水平溢出。
- 验证了不完整拼音、大小写不敏感的首字母、纯数字编号专用匹配、1–10 级星形稀有度、无词条隐藏和有词条完整展示。
- 控制台错误和警告均为 0，可见图片无破图，全部运行时资源仅来自本地预览源。
- 生产预览成功启动于 `127.0.0.1:4173`；回归结束后已关闭 Playwright 会话、终止受管服务，并确认端口未监听且 URL 已失活。

### 2026-07-30

- `BUG-001`、`BUG-002`、`BUG-003` 修复后，12 个测试文件、60 项测试全部通过，TypeScript、数据校验和 Vite 生产构建成功。
- 五套主题的属性文字及选中态对比度均达到 4.5:1；属性图标保持原身份色，属性标签、筛选状态、详情和技能徽章随主题变化。
- Playwright 覆盖五套主题及 1440×900、1152×720、1366×768、390×844，确认应用版本、排序文案、键盘关闭、无横向溢出和无破图。
- 控制台错误和警告为 0，运行时资源仅来自 `127.0.0.1:4173`；回归结束后已关闭浏览器会话、终止受管服务，并确认端口和 URL 均已失活。

### 2026-07-31

- `BUG-004`、`BUG-005`、`BUG-006` 修复后，12 个测试文件、75 项测试全部通过；TypeScript、300 个帕鲁/44,851 条配方的数据校验和 Vite 生产构建成功。
- Playwright 覆盖五套主题及 1440×900、1152×720、1366×768、390×844；工作适性文字实际对比度为 7.03–9.49:1，配种卡片渐变、表面和边框均随主题变化。
- 单亲查询实测 299 条结果、每页 50 条；A/B 任一单选、另一亲本/子代过滤、无结果、翻页、精确双亲切换、清空、键盘主题切换和刷新恢复均通过。
- 四种视口的图鉴、详情和配种页均无横向溢出或可见破图；干净浏览器会话为 0 条控制台错误、0 条警告，运行时请求全部来自 `127.0.0.1:4173`。预览服务成功启动，回归后已关闭浏览器、终止受管服务，并确认端口与 URL 均已失活。
- 首次 EXE 发布门发现 smoke 仍断言已退场的代数上限，更新为当前五主题设置页断言后重新执行成功；Electron 安全配置和自定义协议未改动。
- 最终便携包 `PalTools-0.1.0-win-x64.exe` 为 87,657,181 字节，SHA-256 为 `FD359F315D3E272FDFF2AD6075F90F4C2604A11B376567A7D8C7B0EFB5B6A82D`；打包应用 smoke 与 `en-US`、`zh-CN` 语言包断言通过。
- `BUG-007`、`BUG-008`、`BUG-009` 修复后，12 个测试文件、75 项测试再次全部通过，TypeScript、数据校验和 Vite 生产构建成功。
- Playwright 复核五套主题的详情弹窗表面，并覆盖 1440×900、1152×720、1366×768、390×844；四种视口均无横向溢出或破图，主动技能标题随滚动离开视口，图鉴与配种搜索输入均关闭拼写检查。
- 最终干净浏览器会话为 0 条控制台错误、0 条警告和 0 个第三方运行时资源；预览服务成功启动，回归后已关闭浏览器、终止受管服务，并确认 `127.0.0.1:4173` 端口和 URL 均已失活。

### 2026-07-31（REQ-003）

- 主题与设置测试扩充后，12 个测试文件、107 项测试全部通过；TypeScript、数据校验和 Vite 生产构建成功。
- Playwright 覆盖八套主题及 1440×900、1152×720、1366×768、390×844：均无横向溢出、无破图，刷新后主题正确恢复，设置页方向键从森林夜色切至珍珠白通过。
- 干净浏览器会话为 0 条控制台错误、0 条警告，运行时请求全部来自 `127.0.0.1:4173`；预览服务成功启动，回归后已关闭浏览器、终止服务，并确认端口与 URL 均已失活。
- 截图存放于 `output/playwright/req3-*.png`；本次为 CSS/主题改动，不重复执行 EXE 打包。

### 2026-07-31（REQ-004）

- `cli/` 新增 4 个测试文件、37 项 CLI 测试全部通过；完整测试升至 16 个测试文件、144 项全部通过。
- TypeScript 类型检查、数据校验（300 个帕鲁 / 44,851 条配方）和 Vite 生产构建通过。
- `npm run cli:build` 生成 `build/cli/paltools.mjs`，`node build/cli/paltools.mjs --version` 与 `script/paltools.cmd --version` 均输出 `0.1.0`。
- 真实数据冒烟通过：`info`、`search --element fire --limit 3`、`forward --parents SheepBall,PinkCat --json`（1 条）、`reverse --target ChickenPal --json`（46 条）。
- 退出码实测：未知命令 2、身份歧义 2、无结果 3、数据目录缺失 4、无效方案文件 1；`--data-dir` 指向不存在目录时明确报数据不可用。
- 本次为独立 Node CLI 模块，不涉及 UI 或数据 Schema 变化，不重复执行 EXE 打包。

### 2026-08-01（REQ-005 / REQ-006）

- 删除“琥珀橙”后固定提供七套主题；旧 `amber` 偏好安全回退到森林夜色。七套主题分别完整声明配种图工具栏表面、图标、边框、悬停、激活、禁用和危险操作令牌。
- 具名已有帕鲁预设、方案关联和旧已有帕鲁迁移停止消费与展示；兼容期对象存储和旧键不主动删除。用户可直接从全量可搜索的“加入帕鲁”侧栏拖放或按按钮创建重复节点。
- 配种图占满应用顶栏以下窗口，方案条保持紧凑；桌面侧栏可伸缩，窄屏为覆盖抽屉，文本关系列表可折叠，画布工具统一使用带 Tooltip、键盘名称及激活/禁用状态的图标按钮。
- 完整验证为 22 个测试文件、180 项测试全部通过；TypeScript、300 个帕鲁/44,851 条配方的数据校验和 Vite 生产构建通过。
- Playwright 覆盖 1440×900、1152×720、1366×768 和 600×720；确认侧栏开合、中心加入节点、自动保存、七主题令牌、可见图片和工具栏语义均正常，无横向溢出、控制台错误或第三方运行时请求。验收截图在确认几何断言后作为中间产物清理。
- Windows 便携包及真实打包应用 smoke 通过；`PalTools-0.1.0-win-x64.exe` 为 87,718,436 字节，SHA-256 为 `FA83D5B433F1277676CD739F0CA7FB76D962F30DDC690FD263484B5CCD6A975A`。
