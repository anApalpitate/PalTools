---
schema_version: 1
id: tasks-backlog
title: PalTools 待办
summary: 下一阶段仅优化现有功能的可靠性、性能与维护成本，九项优化按顺序实施，耗时记录已完成，测试分类进行中。
type: task
status: current
authority: supporting
domains: [product, tooling]
topics: [requirements, architecture, testing]
platforms: [shared]
source_of_truth: [docs/reference/01-product-requirements.md, docs/reference/04-roadmap.md, docs/reference/03-architecture.md, package.json, tsconfig.node.json, src, script]
related: [tasks-index, roadmap, architecture]
---

# PalTools 待办

## 当前状态

2026-09-06 完成的架构与文件编排批次已[归档](../archive/2026-09-06-architecture-and-file-orchestration.md)，长期行为已提升到对应 reference/decision 页面。类型检查覆盖、协议共用、运行时数据契约、加载恢复、配种面板与样式拆分、共享焦点管理和浏览器回归入口均已具备，本轮不重复立项。

本阶段由用户明确限定为现有功能优化。下列九项按表中状态推进，不新增页面、业务能力、模型厂商、检索来源、账号、云同步或数据格式；不引入通用状态框架、数据库替换、向量服务或大规模目录迁移。性能项先记录基线，只有收益可测且行为等价才保留实现。

## 实施顺序与验收

优先级按可靠性、性能、维护成本排序。成本为相对估计；同文件事项应顺序实施，实施前重新核对未提交改动。

| 编号 | 状态 | 优先级 | 优化事项 | 修改边界与成功标准 | 成本 |
| --- | --- | --- | --- | --- | --- |
| OPT-09 | 已完成 | 最高 | 改进 Agent 开发耗时记录与卡点分析 | 区分活跃执行、工具等待、用户等待、中断和并行阶段；记录验证子步骤耗时，避免重叠阶段重复累计。任务结束时检查未闭合阶段，并输出可复核的墙钟耗时、活跃耗时和主要卡点。 | 低至中 |
| OPT-01 | 待实施 | 高 | 保证助手消息与证据写入原子性 | 消息、证据和轨迹全部校验后才开始写入；事务失败不残留部分记录。助手存储失败时保留可恢复错误，错误记录再次写入失败也不得造成未处理拒绝。 | 低至中 |
| OPT-02 | 待实施 | 高 | 统一配种工作区写入顺序与生命周期 | 收藏、方案修改、导入替换和重置共用有序写入路径；重试或卸载后不发布旧写入结果。验证延迟提交与导入交错、失败后继续操作时，UI 与 IndexedDB 始终一致。 | 中 |
| OPT-03 | 待实施 | 高 | 收紧模型配置保存与刷新一致性 | Electron 配置读改写串行化，文件采用失败时保留旧文件的替换方式；持久状态与会话密钥变更协调提交。配置刷新只接受最新请求结果，重试具有真实加载状态。 | 中 |
| OPT-04 | 待实施 | 中 | 按会话读取助手证据并释放数据库连接 | 利用现有 messageId 索引读取当前会话的证据和轨迹，移除全库扫描；补齐 AgentRepository 生命周期与卸载释放。保留数据库名称、版本、对象仓库及历史数据兼容性。 | 低至中 |
| OPT-05 | 待实施 | 中 | 拆分助手工作台职责并减少流式重绘 | 将会话编排、发送框与对象选择、消息列表、证据展示按职责拆开；保留一个明确的请求/保存代次所有者。测量并减少流式事件引起的历史消息及无关侧栏重绘，不改变交互。 | 中 |
| OPT-06 | 待实施 | 中低 | 减少本地检索的重复词频扫描 | 建索引时缓存字段词频和文档 ID 映射，复用当前分词与评分规则；比较优化前后的结果顺序、分数、命中字段与耗时。无明确收益时保留原实现并记录测量结论。 | 低至中 |
| OPT-07 | 待实施 | 中低 | 减少验证链中的重复构建 | 先统计协议产物构建与 Web build 的重复次数和耗时，再优先在同一次受管验证流程内复用已校验产物。独立入口仍自足，不降低契约测试、数据校验、类型检查或任一 smoke 的覆盖。 | 中 |
| OPT-08 | 实施中 | 高 | 划分测试类别，按改动范围只运行相关项 | 建立测试分类、改动影响映射和分类执行入口，让日常迭代与普通交付默认运行受影响测试及必要集成检查；覆盖共享依赖的消费者，范围无法判定时回退扩大测试。记录选中理由与耗时，更新执行约束以避免机械全量重跑。 | 中 |

## 审阅依据与定点验证

OPT-09 已完成，原始要求见[归档](../archive/2026-09-08-opt09-agent-timing.md)；命令与计时规则已提升到[快捷命令](../reference/07-quick-commands.md)。

### OPT-01：助手存储原子性与失败恢复

- 依据：[`appendMessage`](../../src/storage/agent-storage.ts) 在 `messages.put` 后才逐项校验证据和轨迹；校验抛错时没有统一中止事务。[助手发送流程](../../src/features/assistant/AssistantPage.tsx)在异常分支再次调用 `appendMessage`，该调用失败没有单独恢复路径。
- 已复现：以独立 `fake-indexeddb` 工厂调用生产仓储，传入合法消息和非法证据，结果为调用拒绝、消息数 1、证据数 0。探针只使用内存数据库，未访问用户数据；这证明部分写入风险，不代表常规合法回答必然失败。
- 定点验收：非法首条/后续证据、非法轨迹、事务中止与配额失败均不能留下半条回答或提前更新会话元信息；持续存储失败时发送状态复位，无未处理 Promise 拒绝。覆盖仓储测试与助手组件测试。

### OPT-02：配种工作区写入队列

- 依据：[`useBreedingWorkspace`](../../src/features/breeding/useBreedingWorkspace.ts) 的普通 `mutate` 使用 `queueRef`，`replaceWorkspace` 直接执行仓储替换，二者不共享顺序；提交完成后也没有加载代次检查。风险依据为源码路径，尚未动态复现。
- 实施：将替换、重置纳入同一队列，明确入队、重试、卸载与数据库关闭的顺序；已提交的事务不伪装成可取消，过期回调不得覆盖当前状态。
- 定点验收：人为延迟 commit/replace/load，覆盖普通修改与导入交错、排队期间重试、卸载及写入失败；队列失败后仍可继续，重新读取与最终界面状态一致。优先补 hook 测试，并回归现有导入确认流程。

### OPT-03：模型配置一致性

- 依据：[`agent-gateway.cjs`](../../script/electron/agent-gateway.cjs) 多个 IPC 入口分别 `readState → writeState`，`writeState` 直接覆盖目标 JSON，保存过程中先改 `sessionKeys` 再落盘；[`useProviderProfiles`](../../src/hooks/useProviderProfiles.ts) 的刷新没有过期结果隔离，重试不重新设置 loading。并发与磁盘失败风险尚未动态复现。
- 实施：集中现有配置更新路径，包含旧状态规范化及密钥重加密写回；序列化同进程更新，使用同目录临时文件和受控替换，不扩展为跨设备同步。维持脱敏、端点密钥作用域和开发者托管配置边界。
- 定点验收：并发保存不同配置、保存与删除/默认项变更交错、落盘失败、乱序加载和重试失败；失败后旧配置仍可读取，密钥与配置不串用，旧刷新不能覆盖新快照。增加本机假数据测试，不请求真实模型或读取开发者密钥。

### OPT-04：会话读取范围与连接生命周期

- 依据：[`loadConversation`](../../src/storage/agent-storage.ts) 已按 conversationId 读取消息，但对 evidence/traces 使用全表 `getAll` 后过滤，现有 messageId 索引仅在删除等路径使用；AgentRepository 在构造时打开数据库，没有 close 接口，助手在渲染期创建仓储。
- 实施：在同一只读事务中按当前消息 ID 发起索引请求；仓储创建、关闭与组件生命周期协调，处理 StrictMode 重挂载和 versionchange，不改变持久化格式。
- 定点验收：多会话样例下结果与原实现一致，读取量随当前会话增长而非全库增长；无关会话的记录不进入当前会话解析；反复进入/离开助手、关闭与读取交错不泄漏连接或破坏在途操作。

### OPT-05：助手工作台职责与渲染

- 依据：[`AssistantPage.tsx`](../../src/features/assistant/AssistantPage.tsx) 同时承担会话读取、生成/取消、保存、模型切换、对象选择和多栏 JSX；每个文本增量更新页面状态，历史消息在同组件内 map 渲染。
- 实施：先完成 OPT-01/03/04 的边界收紧，再按现有职责提取有限组件与 hook；避免按行数机械拆分。以长会话和固定流事件样例记录 React 提交次数与耗时，再决定是否需要增量批处理或展示组件 memo。
- 定点验收：切换会话、取消生成、首次授权、对象引用、模型保存、抽屉焦点与路由行为保持等价；最终文本不丢失、不重复、不串会话。既有组件测试与真实浏览器回归均通过，性能收益有前后对照。

### OPT-06：本地检索计算复用

- 依据：[`LocalKnowledgeService.search`](../../src/domain/knowledge.ts) 对每个查询词在字段 tokens 中反复 filter 计数，`evidenceForEntity` 对非帕鲁实体线性查找文档；这些信息可在现有索引建立时一次计算。
- 定点验收：使用固定的名称、编号、拼音、技能、掉落与无结果查询集，对比排序、分数、matchedFields 与截断规则；分别测量建索引和重复查询的成本。沿用离线轻量检索，不改变召回策略，不增加外部服务。

### OPT-07：验证链构建复用

- 依据：[`package.json`](../../package.json) 中 build 会生成并测试协议产物，随后 smoke:electron 再生成，打包入口也再次生成并测试；[`test-browser.mjs`](../../script/test-browser.mjs) 与 verify:electron 分别重建 Web。[快捷命令](../reference/07-quick-commands.md)明确当前尚无受验证的跳过构建机制。
- 实施：优先复用同一次协调流程中的成功步骤，不引入通用构建缓存系统。若跨命令复用确有收益，必须验证源码、配置、锁文件、输入数据与产物的一致性，不能只检查 HEAD 或文件存在；各独立命令保持安全默认行为。
- 定点验收：记录构建次数及命令耗时；输入未变时避免重复，源码/配置/数据变化、产物缺失或上一步失败时必须重建或拒绝复用。浏览器、源码桌面和打包应用 smoke 仍各自执行；涉及打包链的实现最终需要真实发布门验证。

### OPT-08：测试分类与按影响范围执行

- 用户目标：每次测试更简单，只测试相关项以节省时间。OPT-08 决定本次需要哪些测试，OPT-07 负责已选验证步骤中的构建复用；两项衔接，不重复实现选择逻辑。
- 分类：先按文档、纯领域、存储与异步 hook、React 组件、数据流水线、CLI、模型协议与 Electron、构建脚本划分测试；真实浏览器回归再按图鉴、配种、助手、主题/共享交互场景选择。保留现有测试位置，优先使用配置和现有 runner 能力，不为分类大规模搬迁文件或引入测试平台。
- 选择规则：建立“修改文件/模块 → 相关测试 → 受影响消费者/集成场景”的可维护映射，包含未提交、新增、删除和重命名文件；共享契约、主题、公共 hook、配置和测试辅助代码变化必须扩展到相应消费者，不能只按同名测试或所在目录选择。无法识别的变更需明确提示并扩大范围，不能静默跳过。
- 执行入口：提供可单独运行各类测试的明确入口，并支持根据改动输出本次测试集合及原因；让普通迭代与交付默认采用相关集合。失败修复后先重跑失败项及受影响项，输入未变的已通过检查不重复执行；文档修改只触发文档检查，局部领域修改不启动无关 jsdom、浏览器、数据同步或 EXE 打包。
- 集成边界：源码选择之外，明确类型检查、构建、浏览器、Electron smoke 与完整测试各自的触发条件。跨模块或影响范围无法可靠判定时扩大测试；正式发布保留完整测试与真实打包应用 smoke。实施时同步修订 `AGENTS.md` 与[快捷命令覆盖表](../reference/07-quick-commands.md)，消除普通交付一律全量的旧约定；本 TODO 尚未实施前不宣称已有自动选择能力。
- 定点验收：用固定变更样例验证纯文档、单领域模块、共享契约、存储、局部 UI、全局样式、Electron、测试配置及新增/删除/重命名文件的选择结果。既要断言受影响测试不遗漏，也要断言无关类别不执行；覆盖映射失效时的回退行为。比较同一代码状态下全量与相关集合的命令次数、测试数及耗时，验证选择结果能说明原因，不以单纯减少测试数量作为通过标准。

## 分批交付与范围约束

0. 耗时记录前置：优先实施 OPT-09，使后续任务能区分真实开发、命令执行、等待与中断，并可靠比较优化收益。
1. 测试效率前置：随后实施 OPT-08 的分类、影响映射和执行约束，为后续批次减少无关测试；不等待 OPT-07 的构建复用完成。
2. 第一批：OPT-01 → OPT-02 → OPT-03，先处理部分写入、并发覆盖与错误恢复。
3. 第二批：OPT-04 → OPT-05；OPT-06 可独立测量。可靠性测试先固定行为，再调整读取与渲染结构。
4. 第三批：OPT-07，按测得的重复成本选择最小修改，不为节省构建时间绕过验证。

每项按 `AGENTS.md` 和[快捷命令覆盖表](../reference/07-quick-commands.md)选择定点与完整交付门。源码修改前先补与风险对应的负向测试；本次制定 TODO 不代表已经修复或完成全量回归。

本轮审阅时工作区已有助手未配置模型提示、App 编排、样式、组件测试与 Electron/浏览器 smoke 改动；这些属于已有工作，不重复列为新事项。实施 OPT-01/05 前须重新合并当时的助手代码状态。

现按分批顺序实施；每项通过验证后更新状态、提升长期知识并归档原始要求。

新增需求经确认后记录于此；实施完成并通过验证后，继续按 Wiki 契约提升长期知识并归档原始任务记录。
