# 图鉴与配种问题清单

## 说明

- 适用页面：图鉴、帕鲁详情弹窗、配种查询和配种路径规划。
- 适用环境：Windows Electron 版与浏览器版。
- 本文只记录问题与需求，不代表已经开始修复。
- 已完成项目统一放在文末，并使用 `✔` 标记。

## 待处理问题

当前没有仍在本清单中等待处理的问题。路径规划的未明确描述已移除；若后续出现具体问题，应使用可复现步骤、实际结果和期望结果重新登记。

## 已完成 ✔

### ✔ 搜索支持不完整拼音和纯数字图鉴编号

- 中文名称会生成无声调连续拼音和拼音首字母别名，支持大小写不敏感的连续片段匹配。
- 纯数字查询可匹配去除前导零后的图鉴编号，并保留字母后缀条目的前缀匹配。
- 图鉴搜索、配种选择框和反向亲本筛选共用身份搜索规则。
- 中文名、英文名、内部 ID、技能、掉落物、属性和工作适性能力保持不变。

### ✔ 宽屏顶部栏左右过渡

- 顶部栏内容继续与 1440px 主内容区对齐，背景、模糊层和底部边界延伸到整个视口宽度。
- 常见桌面宽度、125% 缩放有效视口和窄屏布局不产生横向溢出。

### ✔ 稀有度五角星展示

- 稀有度固定使用一行 5 颗星展示；1–5 级使用黄色实心星和空心星。
- 6–10 级从左向右用彩色渐变星覆盖黄色星；现有数据中高于 10 的条目饱和显示为 5 颗彩色星，同时保留原始数值的无障碍文本。
- 缺失稀有度时继续显示“暂无数据”。

### ✔ “固有词条”命名与空状态

- 详情页将“固有被动技能”统一显示为“固有词条”。
- 数据为 `null` 或空数组时不渲染标题、空状态或空白区块。
- 有词条时继续展示名称、Rank 和描述。

### ✔ 详情弹窗左右栏独立滚动

- 桌面布局下，左侧详情与右侧主动技能支持独立上下滚动。
- 滚动一侧不会带动另一侧、整个弹窗或背景页面。
- 低高度和窄屏下回退为弹窗内部整体滚动，确保全部内容可访问。
- 滚动条按悬停、键盘聚焦和滚动状态显示，并保持内容布局稳定。

### ✔ 固有词条区域视觉错位

- 固有词条、工作适性和掉落物品区块保持稳定排列。
- 标题、卡片、Rank 和多行描述不再错位、重叠或产生异常留白。
- 无词条时隐藏区块；有一项和多项词条时布局均保持一致。

### ✔ 默认顺序支持调整排列方向

- 默认顺序按图鉴编号处理，并支持升序和降序。
- 无图鉴编号的条目在两个方向下均稳定排在有效编号之后。
- 搜索和筛选变化后保留当前排列方向，重置后恢复图鉴编号升序。

### ✔ 重置按钮视觉不够醒目

- 图鉴筛选区的重置按钮无需悬停即可识别。
- 默认、悬停、按下和键盘聚焦状态均清晰可辨。
- 桌面和窄屏下不会挤压相邻排序控件。

### ✔ 配种结果卡片重复展示子代名称

- 移除卡片底部重复的子代摘要区域。
- 正向配种和反向查询的公式布局均保持清晰。
- 子代头像、名称、角色和完整可访问文本仍然保留。

### ✔ 目标子代选择框过长

- 宽屏下目标子代选择框不再占据过多横向空间。
- 候选面板仍可展示完整名称、英文名和图鉴编号，并支持滚动与键盘选择。
- 1366×768 和窄屏下无水平溢出，控件可恢复全宽。

### ✔ 应用品牌图标

- Electron 应用和便携 EXE 使用新的关联图标。
- 顶栏使用 `public/app-icon-96.png`，favicon 使用 `public/favicon-32.png`。
- Windows 多尺寸图标位于 `script/electron/resources/icon.ico`，母版位于 `script/electron/resources/icon.png`。
- 图标来源、提示词和派生流程已记录在数据与合规文档中。

## 已完成验证记录

2026-07-28 已完成以下验证：

- 定点测试：`src/domain/pals.test.ts`、`src/App.test.tsx` 通过。
- 完整测试、数据校验、TypeScript 类型检查和 Vite 生产构建通过。
- 浏览器回归覆盖桌面、低高度和窄屏尺寸；确认无水平溢出、滚动穿透、控制台错误或破图。
- Electron 打包应用隐藏 smoke 通过。

2026-07-29 已完成本轮验证：

- 最终完整测试为 7 个测试文件、39 个测试全部通过。
- `npm.cmd run build` 通过，包含 300 个帕鲁、44,851 条配方的数据校验、TypeScript 类型检查和 Vite 生产构建。
- Playwright 回归覆盖 1920×1080、1440×900、1366×768、1152×720 和 390×844；确认顶部栏、页面与详情均无水平溢出。
- 验证了不完整拼音、大小写不敏感的首字母、纯数字编号专用匹配、1–10 级星形稀有度、无词条隐藏和有词条完整展示。
- 控制台错误和警告均为 0，可见图片无破图，全部运行时资源仅来自本地预览源。
- 生产预览成功启动于 `127.0.0.1:4173`；回归结束后已关闭 Playwright 会话、终止受管服务，并确认端口未监听且 URL 已失活。
