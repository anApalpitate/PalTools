# 实施路线图

## 阶段 0：环境与需求（已完成）

React、TypeScript、Vite、Vitest、离线静态数据和基础文档。

## 阶段 1：图鉴与完整配种矩阵（已完成）

299 个 paldb 页面、本地帕鲁图、44,851 条固定正式版配方、双亲查询与 Electron 一键打包。

## 阶段 2：图鉴详细数值与展示（已完成）

本地属性图标、伙伴技能说明、详细战斗/生产/移动数值、数值筛选、图形配种公式和模块化离线爬虫。

## 阶段 3：反向配种、路径规划与内容增强（已完成）

- Schema v4 紧凑正反向索引、工作适应性素材与无性别公式。
- 目标子代反查、分页和全结果搜索。
- “已拥有”与“临时起点”、最少代数和精确 1–12 代算法。
- 默认六代、管理员可配置上限、超限只报告准确代数。
- React Flow 路径树、文本步骤与节点替代配方。
- 主动技能、固有被动、掉落物和本地图标。

退出条件：数据、领域、组件、真实浏览器、离线构建和 EXE 冒烟全部通过。

## 阶段 4：配种工作流重构（已完成）

可编辑配种图已完全替换自动路径规划，完整过程记录见 [08-breeding-graph-requirements.md](../archive/08-breeding-graph-requirements.md)：

1. 将“子代反查亲本”改为“获取目标帕鲁”，支持把配方追加到当前方案。
2. 实现亲本在上、子代在下的可编辑无性别配种森林，以及节点合并、工具栏和文本关系列表。
3. 支持方案 JSON 导入导出、自动保存和会话内撤销/重做。
4. 取消具名已有帕鲁预设，改为全量可搜索的“加入帕鲁”侧栏工具。
5. 将配种图改为占满顶栏以下窗口的画布优先布局，并使用七主题独立适配的图标工具栏。

阶段 1 基础建设已于 2026-07-30 完成：旧自动路径工具、Worker 和代数设置已移除；第三入口替换为配种图空状态；图领域 Schema、约束校验、IndexedDB v1 仓储及旧已有帕鲁一次性迁移已落地。

阶段 2 资源管理于 2026-08-01 完成后又按需求退场：具名预设、关联和旧已有帕鲁迁移已停止消费，方案增删改查及失败恢复继续保留。

阶段 3 配种森林核心已于 2026-08-01 完成：全量“加入帕鲁”侧栏可重复添加节点，精确配方关系、确定性分层布局、等价文本列表和 500ms 自动保存已经落地。后续性能优化已用固定槽位 HTML/SVG 森林视口替换受控 React Flow。

阶段 4 编辑闭环已于 2026-08-01 完成：完整画布工具栏、当前会话撤销/重做、受校验的同帕鲁合并与删除、正向/反向配方追加、节点目标查询和返回上下文已经落地。

阶段 5 发布收口已于 2026-08-01 完成：`.paltools-plan.json` 整体导入导出、ID 重建、名称避让、当前数据集配方重映射、文件/节点/关系上限、原子仓储写入和键盘可访问性已经落地。

2026-08-01 完成后续交互精简：删除琥珀橙后固定为七套主题；停止消费预设和关联；配种图改为全窗口画布、可伸缩“加入帕鲁”侧栏、可折叠文本关系列表和主题化图标工具栏。最终 22 个测试文件、180 项测试、TypeScript、数据校验、生产构建、真实浏览器回归和 EXE 打包应用 smoke 全部通过。

2026-08-01 完成配种图性能与交互收口：固定森林槽位替代自由节点画布，轻量 HTML/SVG 视口替代 React Flow；缩放边界统一为 `0.2–1.5`，“适应画布”按实际节点包围盒和可用内边距计算，工具模式同步切换光标。“加入帕鲁”关闭后完全隐藏，配方追加入口改为带可访问提示的紧凑 `+` 按钮。

2026-08-01 完成配方标记与配种图稳定性基线：查询页直接追加配方改为会话级标记/取消标记，配种图右侧栏分为已标记配方和当前方案关系；导出等待最新保存并准确反馈下载已开始；结构内容与视口保存分离并以修订号串行写入；直接加入节点后保持缩放最小平移到可见区域；本地图片、加入列表和图节点增加原生拖拽及文字选择防护。`REQ-010–013` 仍留待后续槽位树阶段。

2026-07-31 补齐双亲查询的单亲浏览能力：只选择任一亲本即可查看、搜索和分页浏览该帕鲁参与的全部配方；精确双亲、单亲展开和目标反查均已支持追加到配种图。

其后候选：栖息地、掉落来源反查、主动技能反查、只读存档导入、被动继承规划。历史分析见 [archive/03-next-phase-recommendations.md](../archive/03-next-phase-recommendations.md)，当前问题与需求以 [todolist.md](../tasks/todolist.md) 为准。

## 2026-08-01 REQ-010–013 交付

分层槽位领域模型、v2 仓储边界、光标/仅平移模式、固定快捷键、左右图标面板、拖放/删除动态反馈和响应式图编辑器均已落地。详细规则见 [`docs/tasks/2026-08-01-layered-slot-graph-logic.md`](../tasks/2026-08-01-layered-slot-graph-logic.md)。

## 长期不做

存档修改、账号系统、云同步、广告、默认遥测和未经确认的素材公开再分发。
