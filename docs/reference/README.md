# 核心参考文档

本目录保存当前行为与未来范围的权威文档，是 agent 的默认查阅入口。

规则：
- 只有“当前真相”可以放在这里：产品范围、架构、数据合规、管线命令、路线图和需求 backlog。
- 任务计划、验证记录等临时内容放 `../tasks/`；完成后有价值结论并入本目录，其余移入 `../archive/`。
- 行为、Schema、命令或目录变化时必须同步更新对应文档。
- Windows/PowerShell 命令撰写与受管服务约定见 `08-powershell-guide.md`。
